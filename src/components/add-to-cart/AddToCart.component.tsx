type AddToCartPropsType = {
    productId: any
}

const AddToCart = ({ productId }: AddToCartPropsType) => {
    return (
        <button>
            Add To Cart
        </button>
    )
}

export default AddToCart;